server:
  port: 8189
  servlet:
    context-path: /market
spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:mydatabase;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: false

jwt:
  secret: sdlfnDweorJh23ou-3[2k',wPe;fd-acarqE3tuil
  lifetime: 3600000

market:
  version: 13
  app-name: Market v.${market.version}
