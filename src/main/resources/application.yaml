server:
  port: 8189
  servlet:
    context-path: /market
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:mydabase;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    show-sql: true
    prooerties:
      hiibernate:
        dialect: org.hibernate.dialect.H2Dialect
        h2:
          console:
            enabled: true
            setting:
              web-allow-others: false
